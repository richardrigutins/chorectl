{
  "name": "chorectl",
  "private": true,
  "description": "Local dev tooling (commitlint) for the chorectl .NET CLI",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.0"
  }
}
